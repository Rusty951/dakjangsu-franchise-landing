# 닭장수후라이드 가맹 랜딩페이지 PRD

## 1. 제품 개요

닭장수후라이드 가맹 랜딩페이지는 Instagram, 블로그, 광고 등 외부 유입을 카카오톡 상담 또는 리드 폼 문의로 전환하기 위한 단일 목적 페이지다.

이 페이지는 치킨 맛 자체를 먼저 설득하기보다 예비 창업자가 가장 먼저 떠올리는 불안인 "손님이 올까?"를 출발점으로 삼는다. 닭장수후라이드는 배달만 기다리는 치킨집이 아니라 포장 손님과 동네 단골을 함께 쌓아가는 후라이드 치킨 창업 모델로 제시한다.

## 2. 목표와 성공 기준

### 목표

- 예비 창업자가 "내 동네에서도 가능한지 상담받아봐야겠다"라고 느끼게 한다.
- 첫 화면에서 가맹 랜딩페이지임을 명확히 전달한다.
- 카카오톡 가맹 상담을 가장 중요한 행동으로 유도한다.
- 보조 경로로 리드 폼 문의를 제공한다.

### 성공 기준

- 사용자는 첫 화면에서 창업자 문제, 브랜드 답변, 상담 CTA를 바로 이해할 수 있어야 한다.
- 페이지는 승인되지 않은 매출, 수익, 비용, 매장 수, 폐점률 수치를 노출하지 않아야 한다.
- 모바일 화면에서 CTA와 리드 폼 접근이 쉬워야 한다.
- 리드 폼은 필수 정보를 검증하고 개인정보 동의 없이는 제출되지 않아야 한다.
- 상담 클릭, 폼 시작, 폼 제출, 성공, 오류 이벤트를 추적할 수 있어야 한다.

## 3. 역할 분담

이 프로젝트는 Antigravity와 Codex가 같은 요구사항을 보되, 담당 영역을 분리해 작업한다. 경계가 애매한 작업은 먼저 이 섹션을 기준으로 판단한다.

### Antigravity 담당

- 랜딩페이지 프론트엔드 UI 구현
- 섹션 레이아웃, 반응형 화면, 모바일 CTA 접근성
- 시각 계층, 컴포넌트 스타일, 인터랙션, 모션, 최종 화면 polish
- 브랜드 색상, 폰트, 로고, 이미지 배치, alt 텍스트 적용
- 리드 폼의 화면 상태 UI: idle, loading, success, validation error, server error
- 프론트엔드에서 필요한 CTA 클릭 핸들러와 폼 제출 연결부 구현

### Codex 담당

- 제품 요구사항, 정보 구조, 문서 정리
- 리드 폼 데이터 계약과 `POST /api/leads` 구현 또는 검토
- 서버 측 필수 필드 검증, 전화번호 검증 및 정규화, 개인정보 동의 검증
- UTM, referrer, landing path, user agent, submitted_at 캡처 계약
- 트래킹 이벤트 이름과 발생 지점 검토
- 코드 리뷰, 디버깅, 빌드/테스트/QA, 최종 런칭 점검
- 비밀 값이 브라우저나 저장소에 노출되지 않는지 확인

### 공동 확인 항목

- 7단계 페이지 흐름이 제품 의도와 맞는지 확인한다.
- 카카오톡 상담이 1차 CTA로 유지되는지 확인한다.
- 금지 표현과 승인되지 않은 숫자가 화면에 노출되지 않는지 확인한다.
- 런칭 전 TBD 항목이 확정되었는지 확인한다.

### 작업 경계 규칙

- Antigravity는 API 계약, 서버 검증, 비밀 값 처리 방식을 임의로 바꾸지 않는다.
- Codex는 프론트엔드의 시각 디자인과 레이아웃 polish를 임의로 대규모 변경하지 않는다.
- 리드 폼 필드명, 트래킹 이벤트명, CTA 우선순위 변경은 두 담당 영역 모두에 영향이 있으므로 문서 업데이트 후 반영한다.
- 확정되지 않은 KakaoTalk URL, 전화번호, 개인정보처리방침 URL, 리드 수신처는 임의 값으로 채우지 않고 `TBD`로 둔다.

### 프론트엔드 스택 결정

- Antigravity는 `src/frontend`에 React + Vite + Vanilla CSS 기반으로 프론트엔드를 구축한다.
- 컴포넌트 단위는 Hero, Problem/Answer, Menu Proof, Local Fit Q&A, Founder Fit, HQ Support, Lead Capture를 기준으로 나눈다.
- Tailwind, UI kit, CSS-in-JS, 별도 프레임워크는 프로젝트 소유자가 승인하기 전까지 추가하지 않는다.
- 빌드 도구 없이 Vanilla HTML/CSS/JavaScript만 사용하는 방향은 선택하지 않는다.

## 4. 대상 사용자

- 치킨집 또는 외식업 창업을 검토하는 예비 창업자
- 기존 매장을 프랜차이즈 브랜드로 전환하려는 자영업자
- 1인 또는 부부 운영이 가능한 소형 매장을 찾는 창업자
- 배달 의존도가 높은 창업보다 포장 손님과 동네 단골 기반 모델을 검토하는 사용자
- 창업이 처음이라 본사 지원과 운영 준비 과정을 중요하게 보는 사용자

## 5. 핵심 메시지

### 대표 문구

`치킨집 창업의 진짜 문제는 손님입니다.`

### 보조 문구 방향

배달앱에 올린다고 손님이 알아서 오지 않고, 광고비를 쓴다고 단골이 저절로 생기지도 않는다. 닭장수후라이드는 배달만 기다리는 치킨집이 아니라 포장 손님과 동네 단골을 함께 쌓아가는 후라이드 치킨 창업이다.

### 커뮤니케이션 원칙

- 일반적인 "맛있는 치킨" 메시지보다 창업자의 실제 불안을 먼저 다룬다.
- "손님 문제"에서 시작해 닭장수후라이드의 동네형 매장 모델로 답한다.
- 확정되지 않은 성과, 수익, 비용 수치를 설득 근거로 쓰지 않는다.
- 상담을 통해 지역 적합성과 창업 조건을 확인하도록 유도한다.

## 6. 페이지 흐름

페이지는 아래 순서를 기준으로 구성한다.

1. 손님 문제
2. 닭장수의 답
3. 팔릴 메뉴
4. 내 동네 가능성
5. 맞는 창업자
6. 본사 지원
7. 상담

## 7. 섹션 요구사항

### 7.1 Hero

- 첫 화면에서 가맹 창업 랜딩페이지임을 명확히 전달한다.
- 핵심 헤드라인은 `치킨집 창업의 진짜 문제는 손님입니다.`를 기준으로 한다.
- 포장 손님, 동네 단골, 후라이드 치킨 창업 모델을 보조 문구로 설명한다.
- 1차 CTA는 `카카오톡으로 내 지역 창업 상담하기`로 둔다.
- 실제 브랜드, 제품, 매장 이미지가 확보되면 우선 사용한다.

### 7.2 Problem / Answer

- 문제는 "배달앱 등록", "광고비", "막연한 유동인구"만으로 손님이 생기지 않는다는 점에서 출발한다.
- 답은 포장 수요와 반복 구매를 만들 수 있는 동네형 후라이드 치킨 모델로 제시한다.
- 공포를 과장하기보다 창업자가 이미 느끼는 현실적인 불안을 언어화한다.

### 7.3 Menu Proof

- 시그니처 후라이드 치킨을 중심으로 메뉴 경쟁력을 보여준다.
- 표현 가능한 근거는 특제 파우더, 깨끗한 기름, 손질 방식, 얇고 바삭한 튀김옷, 겉바속촉 식감, 고소하고 부드러운 속살이다.
- 최종 사진 또는 영상이 확보되기 전에는 일반적인 스톡 이미지처럼 보이는 자료를 핵심 근거로 쓰지 않는다.

### 7.4 Local Fit Q&A

- 사용자가 "우리 동네에서도 될까?"를 자연스럽게 확인하도록 구성한다.
- 지역, 상권, 포장 수요, 기존 매장 전환 가능성 같은 상담 주제를 다룬다.
- 확정 답변이 필요한 항목은 상담으로 연결한다.

### 7.5 Founder Fit Checklist

- 소형 매장을 고려하는 사용자
- 1인 또는 부부 운영을 고려하는 사용자
- 외식업 또는 프랜차이즈 창업이 처음인 사용자
- 기존 매장 전환을 검토하는 사용자
- 배달만이 아니라 포장과 단골 기반 운영을 원하는 사용자

### 7.6 HQ Support

- 오픈 준비, 운영 관리, 마케팅 지원을 다룬다.
- 지원 범위는 구체적으로 보이되 승인되지 않은 금액, 성과, 보장성 문구는 넣지 않는다.
- 본사가 무엇을 함께 준비하고 확인하는지 실무적으로 전달한다.

### 7.7 Lead Capture

- 카카오톡 상담 CTA를 다시 강조한다.
- 리드 폼은 짧고 명확하게 구성한다.
- 제출 전 개인정보 수집 및 이용 동의를 반드시 받는다.
- 제출 성공 시 담당자가 희망 지역과 창업 조건을 확인한 뒤 연락한다는 안전한 메시지를 보여준다.

## 8. CTA 정책

- 1차 CTA: 카카오톡 가맹 상담
- 2차 CTA: 리드 폼 문의
- 전화번호: 보조 연락 수단

CTA 문구는 사용자의 다음 행동을 구체적으로 말해야 한다. 예: `카카오톡으로 내 지역 창업 상담하기`.

전화번호는 상담 경로를 보조하는 정보로만 사용하며, 프로젝트 전략이 바뀌기 전까지 주요 버튼으로 격상하지 않는다.

## 9. 리드 폼 및 API 요구사항

새로운 public API는 추가하지 않는다. 리드 폼은 기존 `POST /api/leads` 계약을 따른다.

### 필수 필드

- `name`
- `phone`
- `region`
- `privacyConsent`

### 선택 필드

- `budget`
- `timeline`
- `ownerOperated`
- `currentBusiness`
- `storeSize`
- `preferredContactTime`
- `message`

### 수집할 추적 필드

- `utm_source`
- `utm_medium`
- `utm_campaign`
- `utm_content`
- `utm_term`
- `landing_path`
- `referrer`
- `user_agent`
- `submitted_at`

### 응답 및 오류 기준

- 필수 필드 누락: `400`
- 유효하지 않은 전화번호 형식: `400`
- 요청 제한: `429`
- 내부 오류: `500`
- 오류 응답은 사용자에게 안전한 메시지만 노출한다.
- 전화번호는 서버에서 정규화한다.
- 저장 실패와 알림 실패는 로그에서 구분한다.
- API 키, 웹훅, 시트 또는 CRM 인증 정보는 `.env.local`에만 둔다.

## 10. 트래킹 요구사항

아래 이벤트를 추적한다.

- `cta_kakao_click`
- `cta_phone_click`
- `lead_form_start`
- `lead_form_submit`
- `lead_form_success`
- `lead_form_error`

이벤트는 광고 유입의 상담 전환 흐름을 확인할 수 있도록 CTA, 폼, 성공/실패 지점에 배치한다.

## 11. 브랜드 및 UI 기준

### 색상

- Navy: `#142A4D`
- Gold Brown: `#AB7C64`
- Mint: `#73C6C6`
- Roze Pink: `#EE806D`

### 폰트

- Nanum Square
- Pretendard
- system sans fallback

### 로고

- 공식 조합형 로고를 우선 사용한다.
- 로고를 임의로 재색상, 왜곡, 그림자 처리하지 않는다.
- 복잡하거나 시끄러운 배경 위에 로고를 올리지 않는다.

### 이미지

- 실제 브랜드, 제품, 매장, 조리, 포장, 창업자 인터뷰 이미지가 확보되면 우선 사용한다.
- 일반적인 음식 스톡 이미지처럼 보이는 연출은 피한다.
- 이미지에는 유용한 대체 텍스트를 제공한다.

## 12. 금지 표현 및 주의사항

다음 표현 또는 의미를 사용하지 않는다.

- 매출 보장
- 수익 보장
- 무조건 성공하는 창업
- 업계 최저 창업비
- 배달만으로 안정적인 수익
- 경쟁사 직접 비교
- 평균 매출
- 폐점률
- 매장 수
- 창업비 전면 노출
- 승인되지 않은 지원금 또는 비용 수치

숫자, 성과, 지원 금액, 매장 수, 비용 정보는 클라이언트가 공개 문구를 확정하기 전까지 사용하지 않는다.

## 13. QA 기준

### 제품 검수

- 페이지가 승인된 7단계 흐름을 따른다.
- 첫 화면에서 가맹 랜딩페이지임을 알 수 있다.
- 카카오톡 상담이 1차 CTA로 보인다.
- 폼과 CTA 문구가 결과를 과장하거나 보장하지 않는다.
- 미확정 숫자가 공개 영역에 노출되지 않는다.

### 프론트엔드 검수

- 모바일 360px, 390px, 768px과 데스크톱 1440px에서 확인한다.
- 텍스트가 겹치거나 넘치지 않는다.
- 버튼에 접근 가능한 이름이 있다.
- 폼 상태는 idle, loading, success, validation error, server error를 포함한다.
- 이미지에는 유용한 alt 텍스트가 있다.
- 로고 사용은 브랜드 기준을 따른다.

### 백엔드 검수

- 서버에서 필수 필드를 검증한다.
- 서버에서 전화번호를 검증하고 정규화한다.
- 개인정보 동의 없이는 제출할 수 없다.
- UTM, referrer, path, user agent를 캡처한다.
- 비밀 값은 브라우저에 노출되지 않는다.
- API는 안전한 오류 메시지를 반환한다.

### 최종 검수

- 빌드가 통과한다.
- 설정된 typecheck, lint, test가 통과한다.
- 폼 제출 성공 경로와 실패 경로를 테스트한다.
- 실제 KakaoTalk URL, 전화번호, 리드 수신처, 개인정보처리방침 URL이 확인된다.

## 14. 런칭 전 TBD

아래 항목은 런칭 전 확정되어야 하며, 확정 전에는 임의 값으로 대체하지 않는다.

- KakaoTalk 상담 URL: TBD
- 전화번호 최종 표시값: TBD
- 리드 수신처 및 알림 채널: TBD
- 개인정보처리방침 URL: TBD
- 승인된 숫자, 비용, 지원금 관련 공개 문구: TBD
- 최종 사용 가능 사진 및 영상: TBD
